# Combinator

Combinatorial parsing framework for C++ applications. Provides a DSL-like interface using templates and operator overloads. Parser combinators are composable functions that combine simpler parsers to make more complex ones.
